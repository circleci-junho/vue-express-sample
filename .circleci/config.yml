version: 2.1

executors:
  default:
    docker:
      - image: cimg/node:16.15.1

commands:
  yarn_install:
    steps:
      - restore_cache:
          key: yarn-deps-cache-{{ checksum "package.json" }}
      - run: yarn install
      - save_cache:
          key: yarn-deps-cache-{{ checksum "package.json" }}
          paths:
              - node_modules

jobs:
  test:
    executor: default
    steps:
      - checkout
      - run: cd ~/web
      - yarn_install
      - run: yarn test:unit

workflows:
  main:
    jobs:
      - test
